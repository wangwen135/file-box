package com.wwh.filebox.model;

/** 单次传输的结局 / outcome of a single transfer. */
public enum TransferResult {
    /** 成功 / succeeded. */
    SUCCESS,
    /** 失败或中断 / failed or interrupted. */
    FAILED
}
